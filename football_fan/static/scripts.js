if (document.getElementById("table")) {
    const btnTable = document.getElementById("table");

    btnTable.addEventListener("click", function (event) {
        btnTable.style.background = "#EE9A49";
    });
    
}

if (document.getElementById("scorers")) {
    const btnScorers = document.getElementById("scorers");

    btnScorers.addEventListener("click", function (event) {
        btnScorers.style.background = "#EE9A49";
    });
    
}

if (document.getElementById("assists")) {
    const btnAssists = document.getElementById("assists");

    btnAssists.addEventListener("click", function (event) {
        btnAssists.style.background = "#EE9A49";
    });
    
}

if (document.getElementById("fullTable")) {
    const btnFullTable = document.getElementById("fullTable");

    btnFullTable.addEventListener("click", function (event) {
        btnFullTable.style.background = "#EE9A49";
    });
    
}

if (document.getElementById("fullScorers")) {
    const btnFullScorers = document.getElementById("fullScorers");

    btnFullScorers.addEventListener("click", function (event) {
        btnFullScorers.style.background = "#EE9A49";
    });
    
}

if (document.getElementById("fullAssists")) {
    const btnFullAssists = document.getElementById("fullAssists");

    btnFullAssists.addEventListener("click", function (event) {
        btnFullAssists.style.background = "#EE9A49";
    });
    
}

if (document.getElementsByClassName("my__club")) {
    const myClub = document.getElementsByClassName("my__club")[0];

    myClub.addEventListener("click", function( event ) {
        myClub.style.color = "#EE9A49";
    });
}

if (document.getElementById("postComment")) {
    const btnPostComment = document.getElementById("postComment");

    btnPostComment.addEventListener("click", function (event) {
        btnPostComment.style.background = "#EE9A49";
    });
    
}

if (document.getElementsByClassName("btn__login")) {
    const btnLogin = document.getElementsByClassName("btn__login")[0];

    btnLogin.addEventListener("click", function( event ) {
        btnLogin.style.color = "#EE9A49";
    });
}